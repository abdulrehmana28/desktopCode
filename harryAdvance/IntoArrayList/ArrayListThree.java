package javaCC.harryAdvance.intoArrayList;

public class ArrayListThree {
    public static void main(String[] args) {
        
        /* Methods Used 
         list.add(index: 0, element: 5);  //?used to add element at specified index
         list.clear(); //? this will delete all elements
         list.contains(element); //? this will return true if element is present
         list.indexOf(element); //? this will return the index of the element
         list.remove(element); //? this will remove the element
         list.lastIndexOf(element); //? this will return the last index of the given element
         list.set(index, element); //? this will set the index of the element at the given index
         */
    }
    
}
